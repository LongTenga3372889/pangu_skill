# Pangu 客户诉求处理技能

## 触发规则

- 当用户提出**盘古项目的客户诉求 / 需求 / 改动 / 修复**时启用本技能
- 当用户**输入需求号**（如 cro-3842）时启用：先拉取需求，再走分析流程

---

## 核心流程：拉取需求 + 五阶段 + 一道门禁

```
输入需求号 → ⓪拉取需求 → ①需求分析 → ②需求设计 → ③技术设计 → ⛔人工评审 → ④技术实现 → ⑤验证交付
```

> **核心原则**：设计先行，人工评审通过后才允许进入技术实现。AI 不得一路埋头做到实现。

---

## 阶段⓪ 拉取需求 —— 获取需求完整信息

| 项 | 内容 |
|----|------|
| 触发 | 用户输入需求号（如 cro-3842）|
| 动作 | 运行 `python 拉取需求/pull_requirement.py <需求号>`，读取输出的需求信息 |
| 产出 | 需求完整信息：标题、类型、状态、优先级、报告人、负责人、需求描述、截图 OCR 识别、子任务、评论 |
| 完成标志 | AI 已完整掌握需求内容 |

> 截图 OCR 已由脚本自动识别为文字，图片不落盘。子任务、来源 backlog、自定义字段（如 pro_003 实现方式）是判断技术方案的关键线索。

---

## 阶段① 需求分析 —— 搞清楚"要什么"

| 项 | 内容 |
|----|------|
| 目标 | 还原客户真实诉求，消除歧义 |
| 动作 | 基于拉取的需求，追问：哪个租户？哪个服务/模块？什么业务场景？现状是什么？ |
| 产出 | 一句话需求结论 + 背景 + 期望效果 |
| 完成标志 | 需求清晰、无歧义、双方认可 |

## 阶段② 需求设计 —— 定义"做什么"

| 项 | 内容 |
|----|------|
| 目标 | 把诉求转成功能边界 |
| 动作 | 明确功能点、影响范围、不做的事、验收标准 |
| 产出 | 需求设计说明（功能点 + 边界 + 验收标准） |
| 完成标志 | 功能边界清晰 |

## 阶段③ 技术设计 —— 设计"怎么做"

| 项 | 内容 |
|----|------|
| 目标 | 产出可评审的技术方案 |
| 动作 | 查证代码库/表结构（不瞎猜），确定：影响服务、涉及表/字段、实现方式（Java二开 / 脚本 / 数据修复）、变更隔离方案 |
| 产出 | **技术方案**（固定格式，见下） |
| 完成标志 | 技术方案完整可评审 |

### 分析依据（三者综合，缺一不可）

分析需求、设计技术方案时，必须同时参考以下三方面，互相印证：

1. **Skill 知识**：加载相关 skill（如 `marmot-script`、`pangu`），获取方法论、API 用法、适配器机制、开发规范
2. **代码**：查证 Java 源码（服务、Controller/Service 调用链、Feign 远程调用、实体类），理解现有业务逻辑
3. **数据库字典**：查证表结构（表名、字段、关联关系、schema 归属），理解数据模型

> 仅凭单一来源（只读代码、只查表、只靠 skill）都可能导致误判，必须三者交叉验证。

### 技术方案固定格式

```
1. 需求概述
2. 影响范围（服务 / 模块 / 表 / 字段）
3. 实现方式及理由（Java二开 / MarmotScript / 数据修复）
4. 关键改动点（具体到类 / 表 / 脚本位置）
5. 变更隔离与回滚方案
6. 风险点
```

## ⛔ 人工评审 —— 门禁

- **触发**：技术方案产出后，**必须停下**，等待人工判断
- **评审内容**：影响范围对不对、实现方式选得对不对、有无污染标准产品风险
- **通过** → 进入技术实现
- **不通过** → 回退：
  - 方案不合理 → 回退到阶段③重做技术设计
  - 需求理解错 → 回退到阶段①重新分析
- **红线**：未通过评审，**禁止进入技术实现**

## 阶段④ 技术实现 —— 评审通过后才动手

| 项 | 内容 |
|----|------|
| 动作 | 按技术方案实施：写代码 / 写脚本 / 生成修复 SQL |
| 约束 | 严格按方案执行，偏离方案需回到评审 |

### 实现方式选择

| 诉求表述 | 实现方式 | 位置 |
|----------|---------|------|
| "使用脚本实现" / "用MarmotScript" / "写个脚本" | MarmotScript | `{服务}/src/main/resources/js/` |
| 未明确说脚本实现 | Java 二开 | 独立租户模块（scope=provided） |
| "数据修复" / "回填" / "修数据" | data-repair | 生成修复 SQL |

## 阶段⑤ 验证交付

| 项 | 内容 |
|----|------|
| 动作 | 编译/语法检查、逻辑自测、核对数据 |
| 产出 | 变更说明 + 交付物 |

---

## 约束红线（全流程贯穿）

1. **不瞎猜**：表名 / 字段 / 类名必须查证，一切以代码库为准
2. **不污染标准**：二开必须隔离、可追溯、可回滚
3. **不越门禁**：未评审通过，不写任何实现代码
4. **不泄露**：不提交 / 不回显密码、token 等敏感信息
5. **不留痕**：截图等临时文件用后即删，不落盘
6. **不落盘交付**：交付物（脚本 / SQL / 方案）直接输出到对话，不生成文件
